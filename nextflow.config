process {
  executor = 'local'
  withLabel: docker {
    container = 'chipseq-pipeline'
  }
}

profiles {
  docker {
    docker.enabled = true
    process.container = 'chipseq-pipeline'
  }
}

docker {
  enabled = true
}


